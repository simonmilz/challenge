/**
 * 1.Install json-server(npm install -g json-server)
 * 2. go to server (challenge/server)
 * 3.run json-server(json-server -r routes.json db.json -p 3001)
 *
 **/
export const environment = {
	production: false,
	apiUrl: 'http://localhost:3001/api'
};